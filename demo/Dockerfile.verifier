FROM golang:1.26-alpine AS build
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -trimpath -ldflags="-s -w" -o /out/consize ./cmd/verify

FROM alpine:3.20
COPY --from=build /out/consize /consize
USER nobody:nobody
ENTRYPOINT ["/consize"]

